name: RDP

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:

#################################################################
# 1️⃣ Configure Core RDP Settings
#################################################################
    - name: Configure Core RDP Settings
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                           -Name "fDenyTSConnections" -Value 0 -Force
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "UserAuthentication" -Value 0 -Force
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "SecurityLayer" -Value 0 -Force
        netsh advfirewall firewall delete rule name="RDP-Tailscale"
        netsh advfirewall firewall add rule name="RDP-Tailscale" `
          dir=in action=allow protocol=TCP localport=3389
        Restart-Service -Name TermService -Force

#################################################################
# 2️⃣ Create RDP User
#################################################################
    - name: Create RDP User with Secure Password
      run: |
        Add-Type -AssemblyName System.Security

        $charSet = @{
            Upper   = [char[]](65..90)
            Lower   = [char[]](97..122)
            Number  = [char[]](48..57)
            Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
        }

        $rawPassword = @()
        $rawPassword += $charSet.Upper | Get-Random -Count 4
        $rawPassword += $charSet.Lower | Get-Random -Count 4
        $rawPassword += $charSet.Number | Get-Random -Count 4
        $rawPassword += $charSet.Special | Get-Random -Count 4

        $password = -join ($rawPassword | Sort-Object { Get-Random })

        $securePass = ConvertTo-SecureString $password -AsPlainText -Force

        $user = Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue

        if (-not $user) {
            New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member "RDP"
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
        }

        echo "RDP_CREDS=$password" >> $env:GITHUB_ENV

#################################################################
# 3️⃣ Install Tailscale
#################################################################
    - name: Install Tailscale
      run: |
        $url="https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
        $file="$env:TEMP\tailscale.msi"
        Invoke-WebRequest $url -OutFile $file
        Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait
        Remove-Item $file -Force

#################################################################
# 4️⃣ Connect Tailscale
#################################################################
    - name: Establish Tailscale Connection
      run: |
        & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=rdp-node

        $ip=$null
        for ($i=0;$i -lt 10;$i++){
          $ip=& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          if($ip){break}
          Start-Sleep 5
        }

        if(-not $ip){exit 1}

        echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV

#################################################################
# 5️⃣ Verify RDP
#################################################################
    - name: Verify RDP Accessibility
      run: |
        Test-NetConnection $env:TAILSCALE_IP -Port 3389

#################################################################
# 6️⃣ Install rclone
#################################################################
    - name: Install rclone
      run: |
        Invoke-WebRequest https://downloads.rclone.org/rclone-current-windows-amd64.zip -OutFile rclone.zip
        Expand-Archive rclone.zip -DestinationPath $env:TEMP
        Copy-Item "$env:TEMP\rclone*\rclone.exe" "C:\Windows\System32\rclone.exe"

#################################################################
# 7️⃣ Restore Previous Session (Targeted Folders)
#################################################################
    - name: Restore Previous Session
      env:
        # Note the 'MYDRIVE' name here must match the command below
        RCLONE_CONFIG_MYDRIVE_TYPE: "drive"
        RCLONE_CONFIG_MYDRIVE_TOKEN: ${{ secrets.GDRIVE_TOKEN }}
      run: |
        $backupDir = "C:\backups"
        if (!(Test-Path $backupDir)) { New-Item $backupDir -ItemType Directory -Force }

        Write-Host "Checking GDrive for rdp-backups/state.zip..."
        
        # Use 'lsf' but catch errors if the folder doesn't exist
        $backupExists = rclone lsf mydrive:rdp-backups --max-depth 1 2>$null | Where-Object { $_ -match "state.zip" }

        if ($backupExists) {
            Write-Host "Backup found. Downloading..."
            rclone copy mydrive:rdp-backups $backupDir --progress
            
            # Ensure the RDP user folder exists before extracting
            if (!(Test-Path "C:\Users\RDP")) { New-Item "C:\Users\RDP" -ItemType Directory -Force }
            
            Expand-Archive "$backupDir\state.zip" -DestinationPath "C:\Users\RDP" -Force
            Write-Host "Session restored successfully."
        } else {
            Write-Host "No previous backup found or folder missing. Starting fresh."
        }

#################################################################
# 8️⃣ Lifecycle & Targeted Backup (Every 10 minutes)
#################################################################
    - name: Lifecycle & Maintain Connection
      env:
        RCLONE_CONFIG_MYDRIVE_TYPE: "drive"
        RCLONE_CONFIG_MYDRIVE_TOKEN: ${{ secrets.GDRIVE_TOKEN }}
        RDP_CREDS: ${{ env.RDP_CREDS }}
      run: |
        $start = Get-Date
        while ($true) {
          $elapsed = (New-TimeSpan -Start $start -End (Get-Date)).TotalMinutes
          Write-Host "RDP Active | Time Elapsed: $([math]::Round($elapsed, 2)) min"

          if ($elapsed -ge 10) {
            Write-Host "Time limit reached. Preparing backup..."

            $sourcePath = "C:\Users\RDP"
            $tempDir = "C:\temp_backup"
            $destZip = "$tempDir\state.zip"

            if (!(Test-Path $tempDir)) { New-Item $tempDir -ItemType Directory -Force }

            # Create the zip. -ErrorAction SilentlyContinue ignores files currently in use by Windows
            Compress-Archive -Path "$sourcePath\*" -DestinationPath $destZip -Force -ErrorAction SilentlyContinue

            Write-Host "Uploading to Google Drive..."
            # Using 'copyto' ensures the file goes exactly where intended
            rclone copy $destZip "mydrive:rdp-backups" --progress

            Write-Host "Triggering next workflow..."
            $uri = "https://api.github.com/repos/${{ github.repository }}/actions/workflows/main.yml/dispatches"
            Invoke-RestMethod -Uri $uri -Method POST -Headers @{
                Authorization = "token ${{ secrets.GH_PAT }}"
                Accept = "application/vnd.github+json"
            } -Body '{"ref":"main"}'

            break
          }
          Start-Sleep 60
        }
